{
  "name": "same-game-heat",
  "version": "1.0.0",
  "private": true,
  "description": "Same Game Heat parlay board builder",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "PORT=4000 nodemon server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
